TODO

Test using:

sudo docker run --privileged --rm --name test -it alpine ping -c 10 8.8.8.8
sudo docker run --privileged --rm --name test -it alpine sh -mc "ping -c 10 8.8.8.8 & fg"

TODO: Test using:

--cap-add ALL --security-opt apparmor=unconfined --security-opt seccomp=unconfined

instead of

--privileged

sudo ./container_check.stp -DKRETACTIVE=100 -c "sudo strace -c -f ping -c 1 8.8.8.8"
